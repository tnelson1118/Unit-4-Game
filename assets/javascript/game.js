$(document).ready(function() {
    /* define generic variables.
    
    1. function to generate a random number between 19 - 120 and reset users score and score counter.
    assign new values to each crystal button between 1-12 which will be
    hidden from the user 
    until they click it.
        
    fucntion that displays the gem points and updates players score when clicked and determines
    if the player has won or lost and updates wins or loses based on what happened, then restarts
    
    */
});